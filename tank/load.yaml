phantom:
  address: host.docker.internal:8080  # for mac
#  address: 172.17.0.1:8080  # for linux
  ammofile: ammo.txt
  load_profile:
    load_type: rps
    schedule: const(100,15s)
  loop: 1
#  timeout: 60s
console:
  enabled: true
telegraf:
  enabled: false
overload:
  enabled: true
  package: yandextank.plugins.DataUploader
  token_file: "token.txt"
  job_name: billing-test
  job_dsc: billing-test
#autostop:
#  autostop:
#    - http(4xx,25%,5s)
#    - http(5xx,25%,5s)
